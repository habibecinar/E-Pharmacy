[build]
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — tüm client-side route'lar index.html'e yönlendirilir
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
